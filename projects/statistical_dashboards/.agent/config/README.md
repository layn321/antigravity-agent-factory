# Project-Specific Agent Configuration

This directory contains configuration files that are unique to the agents and workflows of the Statistical Dashboards project.

## Standard Files
- `llm_routing.json`: (Future) Define which models handle specific dashboard tasks.
- `api_endpoints.json`: (Future) Mapping of internal dashboard data services.
